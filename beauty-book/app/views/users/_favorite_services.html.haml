%ul.list-group
  - @favorited_stylist_services.each do |favorite|
    %li.list-group-item
      = favorite.stylist_service.service.title
      = " with " + favorite.stylist_service.stylist.full_name
      = link_to "Book", new_appointment_path(appointment_service_id: favorite.stylist_service)
      .favorite-button       
        