- if current_user
  %div Welcome, #{link_to current_user.first_name, user_path(current_user)}
  = link_to "Log out", destroy_user_session_path, method: :delete
- else
  = link_to "Sign In", new_user_session_path, :class => "popup", :"data-width" => 600, :"data-height" => 400
  = link_to "Sign Up", new_user_registration_path, :class => "popup", :"data-width" => 600, :"data-height" => 400